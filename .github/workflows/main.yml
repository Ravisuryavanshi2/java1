  deploy:
    runs-on: ubuntu-latest
    needs: build  # This means deploy will only run after the build job finishes

    steps:
    # Step 1: Checkout the current repository from the 'master' branch
    - name: Checkout Current Repository (master branch)
      uses: actions/checkout@v3
      with:
        ref: 'refs/heads/master'  # Ensure we are working with the 'master' branch

    # Step 2: Set up SSH for secure deployment
    - name: Set up SSH key
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        # Ensure the server's SSH key is added to known_hosts
        ssh-keyscan -H ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts

    # Step 3: Debug SSH Connection (Check if we can connect to the server)
    - name: Test SSH Connection
      run: |
        echo "Testing SSH connection..."
        echo "DEPLOY_USER: ${{ secrets.DEPLOY_USER }}"  # Debug: Ensure DEPLOY_USER is set correctly
        echo "SERVER_IP: ${{ secrets.SERVER_IP }}"      # Debug: Ensure SERVER_IP is set correctly
        ssh -i ~/.ssh/id_rsa -o StrictHostKeyChecking=no ${{ secrets.DEPLOY_USER }}@${{ secrets.SERVER_IP }} 'echo SSH connection successful!' || (echo "SSH connection failed" && exit 1)

    # Step 4: Deploy the current repository
    - name: Deploy Current Repository
      run: |
        echo "Deploying current repository to the server..."
        scp -i ~/.ssh/id_rsa target/*.jar ${{ secrets.DEPLOY_USER }}@${{ secrets.SERVER_IP }}:/path/to/deployment/dir  # Adjust deployment path

    # Step 5: Deploy the external repository (java1) artifact
    - name: Deploy External Repository Artifact (java1)
      run: |
        echo "Deploying external repository (java1) artifact..."
        scp -i ~/.ssh/id_rsa repo-java1/target/*.jar ${{ secrets.DEPLOY_USER }}@${{ secrets.SERVER_IP }}:/path/to/deployment/dir  # Adjust deployment path
